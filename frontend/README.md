# Receipt Manager

A Flutter app scaffold for the receipt manager project.

## Getting Started

This workspace contains Flutter app source in `lib/`, widget tests in `test/`, and generated runners for iOS, macOS, and web.

To run the app:

```sh
flutter pub get
flutter test
flutter run
```

If Flutter asks to refresh generated files later, run `flutter create .` from this directory.
